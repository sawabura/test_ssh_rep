#!/bin/bash 
datatime_now=`date "+%Y-%m-%d %H:%M"`
git config --global user.name "sawabura"
git config --global user.email "victormacgrey@gmail.com"
git add *
git commit -m "$datatime_now"
git branch -M main
git remote add origin https://github.com/sawabura/test_ssh_rep.git
git push -u origin main
